/** Injected by Vite's `define` from package.json — see vite.config.ts. */
declare const __APP_VERSION__: string;

/** Build-time backend endpoint, injected by Vite's `define`. Read only through
 *  src/lib/config/serverConfig.ts — never reference this directly. */
declare const __SYNC_SERVER_URL__: string;
